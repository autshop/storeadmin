export type NewProductFormTypes = {
    name: string;
};

export type NewVariantFormTypes = {
    name: string;
    description: string;
};

export type NewCollectionFormTypes = {
    name: string;
    description: string;
};
